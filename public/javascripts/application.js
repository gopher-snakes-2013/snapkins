window.onload = function() {
  runDoodler()
}
// We're telling the document to execute the runDoodler method after it has
// loaded.

// Loaded is defined as all the html, images, css, and javascript has been downloaded
// and interpreted by the browser.
